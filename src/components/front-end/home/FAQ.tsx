import SectionTitle from "@/components/shared/SectionTitle";

const FAQ = () => {
  return (
    <section>
      <SectionTitle title="Frequesntly Asked Questions" />
    </section>
  );
};

export default FAQ;
