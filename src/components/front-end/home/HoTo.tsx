import SectionTitle from "@/components/shared/SectionTitle";

const HoTo = () => {
  return (
    <section>
      <SectionTitle title="How Does This Book Work?" />
    </section>
  );
};

export default HoTo;
